extends Resource

class_name BoardSpaceList

var TESTPROPERTY1 : PropertySpace = PropertySpace.new("Test Property 1", 20, 5, 10, 20, 25, 30, 5, 10)
var TESTPROPERTY2 : PropertySpace = PropertySpace.new("Test Property 2", 25, 7, 12, 22, 28, 35, 5, 10)
var TESTPROPERTY3 : PropertySpace = PropertySpace.new("Test Property 3", 30, 10, 15, 25, 32, 40, 5, 10)




# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
